*Правило для ИИ:* 
При запросах на обработку таблиц с товарами или поиске дистрибьюторов по ASIN, по умолчанию ищи исходные данные (Excel/CSV файлы) в этой директории. Если тебе нужно написать скрипт на Python для обработки данных из этих таблиц, используй библиотеки `pandas` или `openpyxl`.
