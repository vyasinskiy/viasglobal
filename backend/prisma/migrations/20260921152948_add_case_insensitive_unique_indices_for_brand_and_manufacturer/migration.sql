-- ==============================================================================
-- Миграция: Добавление регистронезависимых уникальных индексов для Brand и Manufacturer
-- Назначение: Защита от создания дубликатов с разным регистром (например, 'Safta' и 'SAFTA')
-- ==============================================================================

-- Удаляем старые обычные индексы, если необходимо, или дополняем функциональными индексами
CREATE UNIQUE INDEX IF NOT EXISTS "Brand_name_upper_idx" ON "Brand"(UPPER(TRIM("name")));

CREATE UNIQUE INDEX IF NOT EXISTS "Manufacturer_name_upper_idx" ON "Manufacturer"(UPPER(TRIM("name")));